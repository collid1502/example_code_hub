#!/bin/bash
AGE="28" 
echo "My age is $AGE in 2022" 

# use the {} around a variable name when immediately wanting to add text after it, otherwise its just optional, like so:
ACTION="show"
echo "I am ${ACTION}ing new linkux skills" 